# Required for music video search (free at https://console.cloud.google.com)
YOUTUBE_API_KEY=

# Optional: Redis for multi-instance session sharing, caching, and rate limiting
# Not required for local dev — the server falls back to in-memory state
# REDIS_URL=redis://localhost:6379

# Optional: number of worker threads for song identification (default: 2)
# IDENTIFY_WORKERS=4

# Optional: number of cluster workers in production (default: cpu count)
# WEB_CONCURRENCY=4

# Optional: server port (default: 3001)
# PORT=3001

# Optional: set to false to skip session codes (useful for standalone Pi setups)
# When disabled, the remote connects directly without entering a code
# REQUIRE_CODE=false

# Optional: PostgreSQL for song play tracking and leaderboard
# Not required — play tracking is silently disabled when unset
# Add via Railway Postgres plugin or any Postgres instance
# DATABASE_URL=postgresql://user:pass@host:5432/vynalize
